services:
  # FastAPI Driver Server (Spark Driver)
  fastapi:
    build: ./fastapi
    container_name: fastapi-driver
    network_mode: host
    environment:
      - IPFS_API=http://localhost:5001  # Since both use host network
    volumes:
      - fastapi-temp:/data/chunks

volumes:
  fastapi-temp:
